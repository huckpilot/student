<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" type="text/css" href="/css/styles.css">
</head>
<body>
  <header>
    <h1>List of students:</h1>
  </header>
  <ul class = "students_list">
  <%items.each do |student| %>
    <li>
      <span href = "/students/<% student.id %>"><%= student.name %></span>

      <form action = "/students/<%= student.id %>/edit" class = "edit-form">
        <button class = "edit-button">Edit</a>
      </form>

      <form action = "/students/<%= student.id %>" method = "post" class = "delete-form">
        <input type = "hidden" name = "_method" value = "delete">
        <button class = "delete-button">Delete Student</button>
      </form>
    </li>
    <% end %>
  </ul>
  <a href = "/students/new">Add Student</a>
</body>
</html>